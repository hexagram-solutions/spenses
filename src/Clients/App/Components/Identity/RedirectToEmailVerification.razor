@using Spenses.App.Infrastructure

@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo(Routes.Identity.EmailVerificationRequired());
    }
}

@inherits BlazorState.BlazorStateComponent

<LoadingIndicator Visible="ExpensesState.ExpenseCreating">
    <ModalHeader>
        <ModalTitle>Add expense</ModalTitle>
        <CloseButton/>
    </ModalHeader>
    <ModalBody>
        <ExpenseForm @ref="ExpensesFormRef"
                     Expense="Expense"/>
    </ModalBody>
    <ModalFooter>
        <Button Color="Color.Warning" Clicked="Close">
            Cancel
        </Button>
        <Button Color="Color.Primary" Clicked="Save">
            <Icon Name="IconName.Add" Margin="Margin.Is2.FromEnd"/>
            Add expense
        </Button>
    </ModalFooter>
</LoadingIndicator>

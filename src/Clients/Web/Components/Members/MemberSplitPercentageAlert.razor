<Alert Color="Color.Success" @bind-Visible="IsValid">
    <AlertMessage>
        <Icon Name="IconName.Check" />
        The total default split percentages among members of this home is 100%.
    </AlertMessage>
</Alert>
<Alert Color="Color.Warning" @bind-Visible="IsNotValid">
    <Heading Size="HeadingSize.Is5" TextWeight="TextWeight.Bold">
        The total default split percentages among members of this home is not 100%.
    </Heading>
    <AlertDescription>
        If the default split percentages for all home members does not total 100%, custom split ratios will need to be
        specified for each expense individually. Ensure that you adjust the default split percentage for each home
        member such that they total exactly 100%.
    </AlertDescription>
</Alert>

@code{
    
    [Parameter]
    public bool IsValid { get; set; }

    [Parameter]
    public EventCallback<bool> IsValidChanged { get; set; }

    public bool IsNotValid
    {
        get => !IsValid;
        set => IsValid = !value;
    }

}
